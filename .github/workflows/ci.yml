name: CI

on:
  push:
    branches: [main, master]
    paths:
      - "desktop/**"
      - ".github/workflows/ci.yml"
  pull_request:
    branches: [main, master]
    paths:
      - "desktop/**"
      - ".github/workflows/ci.yml"
  workflow_dispatch:

# Cancel stale runs for the same PR or same branch so only the latest push runs.
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build (${{ matrix.label }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - label: windows-amd64
            os: windows-latest
            goos: windows
            goarch: amd64
            ext: .exe
            ldflags: -s -w -H windowsgui
            cgo: "0"
            artifact: folio-windows-amd64
          - label: linux-amd64
            os: ubuntu-22.04
            goos: linux
            goarch: amd64
            ext: ""
            ldflags: -s -w
            cgo: "1"
            artifact: folio-linux-amd64

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.25.x"
          cache: true

      - name: Install Linux webview dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential pkg-config \
            libgtk-3-dev libwebkit2gtk-4.0-dev \
            || sudo apt-get install -y \
            build-essential pkg-config \
            libgtk-3-dev libwebkit2gtk-4.1-dev

      - name: Vet
        working-directory: desktop
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: ${{ matrix.cgo }}
        shell: bash
        run: go vet ./...

      - name: Smoke test (PDF render)
        working-directory: desktop
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: ${{ matrix.cgo }}
        shell: bash
        run: go run ./cmd/smokepdf

      - name: Build desktop
        working-directory: desktop
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: ${{ matrix.cgo }}
        shell: bash
        run: |
          mkdir -p build/bin
          go build -tags production -ldflags "${{ matrix.ldflags }}" -o "build/bin/folio${{ matrix.ext }}" .

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: desktop/build/bin/folio${{ matrix.ext }}
          if-no-files-found: error

  build-server:
    name: Build (folio-server)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.22.x"
          cache-dependency-path: server/go.sum
      - name: Test and build server
        working-directory: server
        env:
          CGO_ENABLED: "0"
        run: |
          go test ./...
          go build -trimpath -ldflags="-s -w" -o folio-server .

  # Debian Trixie: checkout on the host, compile inside debian:trixie via Docker
  # so the binary links against Trixie webkit/GTK/glibc (matches Debian 13 laptops).
  build-debian-trixie:
    name: Build (debian-trixie-amd64)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build inside debian:trixie
        run: |
          set -euo pipefail
          mkdir -p desktop/build/bin
          docker run --rm \
            -v "$PWD":/src \
            -w /src/desktop \
            -e CGO_ENABLED=1 \
            -e DEBIAN_FRONTEND=noninteractive \
            debian:trixie \
            bash -euxo pipefail -c '
              apt-get update
              apt-get install -y --no-install-recommends \
                ca-certificates curl git \
                build-essential pkg-config \
                gcc g++ \
                libgtk-3-dev \
                libwebkit2gtk-4.1-dev \
                libayatana-appindicator3-dev \
                libglib2.0-dev

              # Install Go matching go.mod (1.25.x)
              GO_VER=1.25.0
              curl -fsSL "https://go.dev/dl/go${GO_VER}.linux-amd64.tar.gz" \
                -o /tmp/go.tgz
              rm -rf /usr/local/go
              tar -C /usr/local -xzf /tmp/go.tgz
              export PATH=/usr/local/go/bin:$PATH
              go version

              # Show webkit pkg-config (helps debug Wails CGO)
              pkg-config --modversion webkit2gtk-4.1 || true
              pkg-config --libs --cflags gtk+-3.0 webkit2gtk-4.1

              # -buildvcs=false: mounted .git often fails "error obtaining VCS status"
              # inside Docker (exit 128 / safe.directory).
              # webkit2_41: Debian Trixie has webkit2gtk-4.1 only (no 4.0 .pc).
              go build -buildvcs=false -tags "production,webkit2_41" -ldflags "-s -w" \
                -o build/bin/folio-linux-amd64-debian-trixie .
              ls -la build/bin/folio-linux-amd64-debian-trixie
            '

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: folio-linux-amd64-debian-trixie
          path: desktop/build/bin/folio-linux-amd64-debian-trixie
          if-no-files-found: error
