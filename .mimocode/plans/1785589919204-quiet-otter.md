# Diagnose: PDF sync not working

## Hypothesis
The sync fires in `closeReader` for both EPUB and PDF. But for PDF, the POST might be failing silently. Add logging to `syncProgressToServer` to see what's happening.

## Fix
Add console.log to `syncProgressToServer` so we can see:
- Whether it's called
- What fingerprint/page/device is sent
- Whether the POST succeeds or fails

### Files:
- `android/app/src/main/assets/www/main.js`
- `desktop/frontend/dist/main.js`
